


int _printf(const char *format, ...)
{

  va_list args;

  va_start(args, format);
  unsigned int i = 0;
  while (format && format[i])
    {
      if (format[i] == '%' && format[++i])
	{
	  
	}
      else
	{
	  
	}
    }




  va_end(args);
}
